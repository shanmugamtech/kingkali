const usernameId = document.getElementById('usernameId');
const passwordId = document.getElementById('passwordId');

function login() {
    if (usernameId === "admin" && passwordId === "Admin@123") {} else {
        alert('UserName Incorrect...Password Incorrect')
    }
}